import React from 'react';
import { Layout } from 'antd';
import './header'

const { Header } = Layout;

const Head = () => (
    <Layout>
        <Header className="header">HEAD</Header>
    </Layout>
);

export default Head;
